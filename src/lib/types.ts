export interface ProjectTypes {
  id: number;
  title: string;
  term: string;
  skills: string[];
  url: string;
  github: string;
  desc: string;
}

export interface DarkModeTypes {
  $isDarkMode: boolean;
}
